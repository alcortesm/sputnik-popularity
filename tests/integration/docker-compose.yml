version: "3.6"
services:

  influxdb:
    image: quay.io/influxdb/influxdb:2.0.0-beta

  tester:
    build:
      context: ../../app
      target: src
    entrypoint: ["go", "test", "./influx", "-tags", "integration", "-cover", "-race"]
    depends_on:
      - influxdb
